import { router } from "expo-router";
import { Linking, Pressable, Text, View } from "react-native";
import { ExternalLink, FileText, Shield } from "lucide-react-native";
import { Screen } from "@/components/Screen";
import { config } from "@/lib/config";

export default function LegalScreen() { return <Screen title="Legal and privacy" subtitle="How ToolKit handles data and the rules for using the service."><Document icon={Shield} title="Privacy policy" detail="Account, profile, social, AI, messaging, media, device, safety, and deletion data." onPress={() => void Linking.openURL(`${config.serverUrl}/policy`)} /><Document icon={FileText} title="Terms of use" detail="Eligibility, user content, acceptable use, moderation, external tools, and availability." onPress={() => void Linking.openURL(`${config.serverUrl}/terms`)} /><Document icon={FileText} title="Account deletion" detail="Immediate hiding, the 30-day recovery window, and final data purge." onPress={() => void Linking.openURL(`${config.serverUrl}/account-deletion`)} /><Text className="rounded-2xl bg-primary/10 p-4 text-sm leading-5 text-primary">ToolKit is intended for ages 13+. Legal copy requires owner/legal approval before store release.</Text><Pressable onPress={() => router.push("/support" as never)} className="min-h-12 items-center justify-center rounded-2xl bg-primary"><Text className="font-bold text-white">Contact support</Text></Pressable></Screen>; }
function Document({ icon: Icon, title, detail, onPress }: { icon: typeof Shield; title: string; detail: string; onPress: () => void }) { return <Pressable accessibilityRole="link" onPress={onPress} className="flex-row items-center gap-4 rounded-3xl border border-border bg-card p-5"><Icon color="#ed4b4b" size={22} /><View className="flex-1"><Text className="font-bold text-foreground">{title}</Text><Text className="mt-1 text-sm leading-5 text-muted-foreground">{detail}</Text></View><ExternalLink color="#6f6a87" size={18} /></Pressable>; }
